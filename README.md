Just a copy of the default number widget with the difference that the number will not be shortened.

Installation:

Copy the `longnumber` folder in you `widgets` folder.

Usage:

`data-view="Longnumber"`